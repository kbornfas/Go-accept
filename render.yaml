services:
  - type: web
    name: p2p-payment-coordinator
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
    healthCheckPath: /api/wallet
    autoDeploy: true
